// This module deals with adding all the event listeners and therefore all the functionality.
// It will be the main one to call DOMManipulator.
import { GameState } from "./gamestate";

// Static methods
class EventListeners {
    static attachPhase1Listeners(gameState) {}
    static attachPhase2Listeners(gameState) {}
    static attachPhase3Listeners(gameState) {}
}

export default EventListeners;